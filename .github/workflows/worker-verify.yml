name: Worker Verify

on:
  push:
    paths:
      - "worker/**"
      - ".github/workflows/worker-verify.yml"
  pull_request:
    paths:
      - "worker/**"
      - ".github/workflows/worker-verify.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: worker
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: worker/package-lock.json
      # The committed lockfile predates workers-types v5. Wrangler's peer on that
      # types-only package is optional; keep the reproducible lock until the
      # dedicated typings migration regenerates it.
      - run: npm ci --legacy-peer-deps
      - run: npm test
      - run: npm run typecheck
      - run: npx wrangler deploy --dry-run
